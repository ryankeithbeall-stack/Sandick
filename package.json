{
  "name": "sandick-contracts",
  "version": "0.1.0",
  "private": true,
  "description": "HyperEVM smart-contract vault for an equal-weighted HIP-3 basket (Sandick).",
  "scripts": {
    "compile": "node scripts/compile_contracts.js",
    "test:contracts": "node contracts/test/vault.test.js",
    "coverage:contracts": "node scripts/coverage_run.js",
    "deploy": "node scripts/deploy.js"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.1.0",
    "solc": "^0.8.26"
  },
  "devDependencies": {
    "@ethereumjs/common": "^10.1.2",
    "@ethereumjs/util": "^10.1.2",
    "@ethereumjs/vm": "^10.1.2",
    "ethers": "^6.17.0"
  }
}
